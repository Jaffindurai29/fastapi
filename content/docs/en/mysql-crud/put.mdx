---
title: "3. PUT"
description: Replacing a row's value entirely, by ID.
---

## The code

```python title="main.py"
from fastapi import Depends, FastAPI, HTTPException
from pydantic import BaseModel
from sqlalchemy.orm import Session

app = FastAPI()

class ItemRequest(BaseModel):
    value: str

@app.put("/items/{item_id}")
def replace_item(item_id: int, item: ItemRequest, db: Session = Depends(get_db)):
    row = db.get(ItemModel, item_id)
    if row is None:
        raise HTTPException(status_code=404, detail="Item not found")
    row.value = item.value
    db.commit()
    db.refresh(row)
    return {"id": row.id, "value": row.value}
```

## Line by line

1. **Look the row up first** — `db.get(ItemModel, item_id)` returns
   `None` if that ID doesn't exist, so a clean `404` goes out instead of
   silently doing nothing.
2. **`row.value = item.value`** — looks like a plain attribute
   assignment because it is one. SQLAlchemy's ORM tracks the change
   automatically; nothing needs to be re-added to the session.
3. **`db.commit()`** writes the change; **`db.refresh(row)`** makes sure
   `row` reflects exactly what's now in the database before it's
   returned.

## Try it

```bash
curl -X PUT http://127.0.0.1:8000/items/1 -H "Content-Type: application/json" -d "{\"value\": \"replaced\"}"
```

Next: [PATCH](/en/docs/mysql-crud/patch) — partially updating a row.
