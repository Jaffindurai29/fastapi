---
title: "2. POST"
description: MySQL-la pudhu row insert pannurathu.
---

## Code

```python title="main.py"
import os

from fastapi import Depends, FastAPI, status
from pydantic import BaseModel
from sqlalchemy import Column, Integer, String, create_engine
from sqlalchemy.orm import Session, declarative_base, sessionmaker

MYSQL_HOST = os.getenv("MYSQL_HOST", "127.0.0.1")
MYSQL_PORT = os.getenv("MYSQL_PORT", "3306")
MYSQL_USER = os.getenv("MYSQL_USER", "root")
MYSQL_PASSWORD = os.getenv("MYSQL_PASSWORD", "")
MYSQL_DB = os.getenv("MYSQL_DB", "fastapi_learn")

DATABASE_URL = os.getenv(
    "DATABASE_URL",
    f"mysql+pymysql://{MYSQL_USER}:{MYSQL_PASSWORD}@{MYSQL_HOST}:{MYSQL_PORT}/{MYSQL_DB}",
)

engine = create_engine(DATABASE_URL, pool_pre_ping=True)
SessionLocal = sessionmaker(autocommit=False, autoflush=False, bind=engine)
Base = declarative_base()


class ItemModel(Base):
    __tablename__ = "crud_post_items"
    id = Column(Integer, primary_key=True, autoincrement=True)
    value = Column(String(255), nullable=False)


Base.metadata.create_all(bind=engine)

with SessionLocal() as db:
    if db.query(ItemModel).count() == 0:
        db.add_all([ItemModel(value="first"), ItemModel(value="second")])
        db.commit()

app = FastAPI()


def get_db():
    db = SessionLocal()
    try:
        yield db
    finally:
        db.close()


class ItemRequest(BaseModel):
    value: str


@app.post("/items", status_code=status.HTTP_201_CREATED)
def create_item(item: ItemRequest, db: Session = Depends(get_db)):
    row = ItemModel(value=item.value)
    db.add(row)
    db.commit()
    db.refresh(row)
    return {"id": row.id, "value": row.value}
```

## Line by line

Mela irukra `MYSQL_*`/`DATABASE_URL` setup, `engine`/`SessionLocal`/`Base`,
`ItemModel` table, `Base.metadata.create_all(...)`, mattrum seed block —
indha topic-la irukra ella page-um share panra same pieces dhan — adha
line-by-line explain panradha [index page](/tanglish/docs/mysql-crud)-la
paarunga. Keezha, indha page-ku specific-a enna irukko adha mattum
paarpom.

1. `db: Session = Depends(get_db)` — [GET](/tanglish/docs/mysql-crud/get)-la
   paartha same dependency injection: indha route body run
   aaguradhukku munnadi FastAPI `get_db()`-ah adhoda `yield`
   varaikkum run pannum, pudhusa create panna session-ah `db`-a
   kudukkum, route mudinja appuram (success-a irundhalum failure-a
   irundhalum) andha session-ah automatic-a close pannum.
2. `row = ItemModel(value=item.value)` — memory-la pudhu ORM object
   build pannudhu; database-la edhuvum ippa write aagala.
3. `db.add(row)` — pudhu object-ah session-oda pending state-la stage
   pannudhu mattum; MySQL-ku edhuvum ippa anuppala.
4. `db.commit()` — andha pending `INSERT`-ah flush pannum,
   transaction-ah commit pannum. Idhu dhan MySQL real-a
   auto-increment `id`-ah assign panra point.
5. `db.refresh(row)` — `row.id` real generated value-ah reflect
   panradhukku, database-la irundhu `row`-ah thirumba query pannudhu.
   Idha skip pannina `row.id` innum stale-a illa `None`-a irukalam —
   SQLAlchemy-ku andha database-generated value thirumba kekkama
   theriyadhu.

## Try it

```bash
curl -i -X POST http://127.0.0.1:8000/items -H "Content-Type: application/json" -d "{\"value\": \"third\"}"
```

Adutha step: [PUT](/tanglish/docs/mysql-crud/put) — oru row-oda value-ah
full-a replace pannurathu.
