---
title: React + MySQL + CRUD
description: Topic 5-oda same React frontend, real MySQL database mela point panni.
---

<GithubLink path="example/7/a" label="Backend" />{" "}
<GithubLink path="example/7/react" label="Frontend" />

## Files

[MySQL + CRUD](/tanglish/docs/mysql-crud)-oda same layered structure —
oru file-ku pathila `database.py`/`models.py`/`schemas.py`/`crud.py`/
`main.py`:

| File | Responsibility |
|---|---|
| `database.py` | env vars, `engine`, `SessionLocal`, `Base`, `get_db()` |
| `models.py` | `ItemModel` — SQLAlchemy table, indha topic-oda sontha `crud_react_items` |
| `schemas.py` | `ItemRequest` mattrum `ItemPatch` — indha app-ku rendum venum, body edukkura ovoru verb-kum onnu |
| `crud.py` | aaru database functions-um — ovoru verb-kum onnu, plus `seed_items` |
| `main.py` | `FastAPI()` app, `CORSMiddleware`, mattrum aaru routes-um |

Indha topic-oda table adhoda sontha `crud_react_items` —
[MySQL + CRUD](/tanglish/docs/mysql-crud)-oda shared `items` table-ku
vera — indha topic run panna, andha topic-oda data-ah touch pannadhu,
adhu maadhiri thirumbavum.

## Shared setup

**Connection, environment variables-la irundhu** —
[MySQL + CRUD](/tanglish/docs/mysql-crud) mathiri dhan, plus
`load_dotenv()` — so local `.env` (`.env.example`-la irundhu copy
pannirundha) automatic-a pick up aagum:

```python title="database.py"
import os

from dotenv import load_dotenv
from sqlalchemy import create_engine
from sqlalchemy.orm import Session, declarative_base, sessionmaker

load_dotenv()

MYSQL_HOST = os.getenv("MYSQL_HOST", "127.0.0.1")
MYSQL_PORT = os.getenv("MYSQL_PORT", "3306")
MYSQL_USER = os.getenv("MYSQL_USER", "root")
MYSQL_PASSWORD = os.getenv("MYSQL_PASSWORD", "")
MYSQL_DB = os.getenv("MYSQL_DB", "fastapi_learn")

# Set DATABASE_URL directly to override everything above (e.g. to point
# at SQLite for a quick local test with no MySQL server at all).
DATABASE_URL = os.getenv(
    "DATABASE_URL",
    f"mysql+pymysql://{MYSQL_USER}:{MYSQL_PASSWORD}@{MYSQL_HOST}:{MYSQL_PORT}/{MYSQL_DB}",
)

engine = create_engine(DATABASE_URL, pool_pre_ping=True)
SessionLocal = sessionmaker(autocommit=False, autoflush=False, bind=engine)
Base = declarative_base()


def get_db():
    db = SessionLocal()
    try:
        yield db
    finally:
        db.close()
```

## Line by line: connection

1. `load_dotenv()` — keezha irukra `os.getenv(...)` call edhuvum run
   aaguradhukku munnadi, local `.env` file irundha (`.env.example`-la
   irundhu copy pannirundha) adha environment-ku load pannum. `.env`
   create pannama vittutta, keezha irukra ella variable-um adhoda
   default-ku fall back aagum.
2. `os.getenv("DATABASE_URL", f"mysql+pymysql://...")` — `DATABASE_URL`
   environment variable set aa irundha adha padikkum (e.g. SQLite
   override), illa-na individual `MYSQL_*` variables-la irundhu MySQL
   connection string build pannum. `mysql+pymysql://` SQLAlchemy-ku
   edhu driver use pannanum-nu sollum — `pymysql` — real-a MySQL-oda
   pesuradhukku.
3. `create_engine(DATABASE_URL, pool_pre_ping=True)` — `engine`
   SQLAlchemy-oda connection factory indha database-ku; session onnu
   real-a connection venumnu kekkura varaikkum edhuvum open pannadhu.
   `pool_pre_ping=True` pooled connection-ah reuse panna munnadi
   lightweight ping vachi test pannum, so MySQL quietly drop panna
   connection replace aagum, apparam confusing error varradhukku
   pathila.
4. `sessionmaker(autocommit=False, autoflush=False, bind=engine)` —
   indha engine-oda bind panna `Session` objects create panna factory.
   `autocommit=False` nu solla, `db.commit()` explicit-a call pannura
   varaikkum database-ku edhuvum write aagadhu; `autoflush=False`
   nu solla, pending changes ovoru query-kum munnadi silent-a
   database-ku pogadhu, flush illa commit pannumbodhu mattum — rendum
   sernthu ovoru route-kum SQL eppo real-a run aagumnu full control
   kudukkum.
5. `get_db()` — request-ku oru session open panra dependency, `yield`
   vachi, apparam eppovum `finally: db.close()`-la close pannum, route
   raise pannalum. Indha topic-oda ovoru route-um `db: Session =
   Depends(get_db)` declare panni idha vaangum — edhavadhu verb
   page-oda line-by-line section-la idha action-la paarunga.

**Table, Python class-a** — real, stable, auto-incrementing `id` vachi,
array topic-oda position-based index mathiri illama:

```python title="models.py"
from sqlalchemy import Column, Integer, String

from database import Base


class ItemModel(Base):
    __tablename__ = "crud_react_items"

    id = Column(Integer, primary_key=True, autoincrement=True)
    value = Column(String(255), nullable=False)
```

## Line by line: table

1. `class ItemModel(Base):` — oru ORM model declare pannudhu.
   `Base`-la (indha `database.py`-la irundhu import panna) inherit
   panradhu dhan indha class-ah real database table-a map pannudhu,
   plain Python class mathiri illama.
2. `__tablename__ = "crud_react_items"` — indha topic-oda sontha
   table, [MySQL + CRUD](/tanglish/docs/mysql-crud)-oda shared `items`
   table-la irundhu vera vachirukku, so indha topic run panna andha
   topic-oda data-oda collide aagadhu.
3. `id = Column(Integer, primary_key=True, autoincrement=True)` —
   real, stable, auto-incrementing integer primary key, ovoru
   insert-layum MySQL thanne assign pannum.
4. `value = Column(String(255), nullable=False)` — required
   `VARCHAR(255)` column; `nullable=False` nu solla, idha vittutta
   panra insert-ah MySQL reject pannum.
5. `main.py` startup-la `Base.metadata.create_all(bind=engine)` call
   pannum, table illainaa create pannum — everytime app start aagum
   pothu call panna safe, table already irundha no-op.

## Backend

[MySQL + CRUD](/tanglish/docs/mysql-crud)-oda aaru routes-um, oru
app-a sernthu — [Array + CRUD](/tanglish/docs/array-crud)-oda
routes [React + Array + CRUD](/tanglish/docs/react)-kaga sernthadhu mathiri dhan —
plus andha topic-oda same `CORSMiddleware`, `app` create panna
udane `main.py`-la register pannirukom:

```python title="main.py"
app.add_middleware(
    CORSMiddleware,
    allow_origins=["http://localhost:5173", "http://127.0.0.1:5173"],
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)
```

## Line by line

1. `app.add_middleware(CORSMiddleware, ...)` — ovoru request/response-kum
   run aagra middleware register pannudhu, oru origin-la irukra
   JavaScript vera origin-oda response-ah padikkanumna, browser
   kekkura HTTP headers add pannum.
2. `allow_origins=[...]` — indha API-ku cross-origin request anuppa
   allow panna rendu origins mattum: Vite dev server, `localhost`/
   `127.0.0.1`, port `5173`. Vera edhuvum request indha backend-ku
   vara munnadiye browser dhan block pannidum.
3. `allow_credentials=True` — cookies/auth headers cross-origin
   anuppa allow pannum. Indha example use pannala, aana aprom
   authentication add panna vendumna venum.
4. `allow_methods=["*"]` / `allow_headers=["*"]` — `GET`, `POST`,
   `Content-Type` nu ovonna ovonna-a list pannuradhukku pathila, ella
   HTTP method-um header-um allow pannudhu.

Vera edhuvum maaradhu — CORS-ku, protect panra routes-oda pinnadi enna
irukku nu kavalai illa.

## Same frontend, oru state rename

Idhu [React + Array + CRUD](/tanglish/docs/react)-la irundha exact same app dhan,
array backend-ku pathila mela irukra MySQL backend mela point pannudhu.
Indha section-oda ovoru page-um direct comparison — diff eppovum same
oru change: **index → id**. Requests-um axios moolama same maadhiri dhan
pogudhu — [Requests: axios](/tanglish/docs/react#requests-axios) paarunga.

```jsx
const [items, setItems] = useState([]);          // database rows, ovonnum { id, value }
const [editingId, setEditingId] = useState(null); // edit aagra row-oda id
```

`editingIndex` → `editingId`. Same idea, ippo enna hold pannudho adha
match panna rename pannirukom — array-la position illa, database ID.
`items`-oda shape-um maari, ovoru entry-um `{ id, value }`.

## Line by line

1. `const [items, setItems] = useState([]);` — array version-oda
   same state, ippo `{ id, value }` objects hold pannudhu, plain
   string illa — MySQL-backed route ovonnum indha shape-la dhan
   return pannum.
2. `const [editingId, setEditingId] = useState(null);` — array
   version-oda `editingIndex`/`setEditingIndex`, indha topic full-a
   `editingId`/`setEditingId`-a rename aachu, adhu enna hold
   pannudho adha match panna — array position illa, database ID.
3. Array version-la `editingIndex === index` compare pannina illana
   `.map()` loop index anuppina edhavadhu idam-la, indha topic
   `editingId === item.id` compare pannum, `item.id`-ah anuppum —
   identifier-ah row object-la irundhu neraa edukkuradhu, list-la
   adhoda position-la irundhu illa.

## Indha topic-la irukra pages

1. [GET](/tanglish/docs/mysql-react/get) — rows load pannurathu, ID vachi oru row reach pannurathu.
2. [POST](/tanglish/docs/mysql-react/post) — add form (maaradhu).
3. [PUT](/tanglish/docs/mysql-react/put) — "Save (PUT)" button, ID vachi address pannurathu.
4. [PATCH](/tanglish/docs/mysql-react/patch) — "Save (PATCH)" button, ID vachi address pannurathu.
5. [DELETE](/tanglish/docs/mysql-react/delete) — row-ah delete pannurathu, mattrum IDs yeandhukku shift aagadhu.

## Indha topic-ah eppadi run pannurathu

[MySQL + CRUD](/tanglish/docs/mysql-crud)-oda same database setup
venum, plus backend mattrum frontend rendum oru time-la run aaga
vendum:

1. **Oru thadava database setup** (andha topic-ku already pannirundha skip pannunga):

   ```bash
   pip install sqlalchemy pymysql python-dotenv
   ```

   ```sql
   CREATE DATABASE fastapi_learn;
   ```

   ```bash
   export MYSQL_HOST=127.0.0.1
   export MYSQL_PORT=3306
   export MYSQL_USER=root
   export MYSQL_PASSWORD=your-password
   export MYSQL_DB=fastapi_learn
   ```

   ...illana `cp example/7/a/.env.example example/7/a/.env` panni
   adha edit pannunga — `database.py` adha automatic-a load pannikum.
   Rendayum skip pannalum sari; melera defaults already baked in.

   MySQL kaiyila illaya? Pathila `export DATABASE_URL="sqlite:///./test.db"` pannunga.

2. **Terminal 1 — backend:**

   ```bash
   cd example/7/a
   uvicorn main:app --reload
   ```

3. **Terminal 2 — frontend:**

   ```bash
   cd example/7/react
   npm install
   npm run dev
   ```

4. Vite print panra URL open pannunga (usually
   [http://localhost:5173](http://localhost:5173)).

Full frontend code indha repo-la `example/7/react`-la irukku (adhoda
`README.md` mattrum `STEPS.md` paarunga).

## Full code

**Backend** — `example/7/a`, aaru files-a split aagi, aaru database
functions-um, aaru routes-um, plus CORS:

```python title="database.py"
import os

from dotenv import load_dotenv
from sqlalchemy import create_engine
from sqlalchemy.orm import Session, declarative_base, sessionmaker

load_dotenv()

MYSQL_HOST = os.getenv("MYSQL_HOST", "127.0.0.1")
MYSQL_PORT = os.getenv("MYSQL_PORT", "3306")
MYSQL_USER = os.getenv("MYSQL_USER", "root")
MYSQL_PASSWORD = os.getenv("MYSQL_PASSWORD", "")
MYSQL_DB = os.getenv("MYSQL_DB", "fastapi_learn")

# Set DATABASE_URL directly to override everything above (e.g. to point
# at SQLite for a quick local test with no MySQL server at all).
DATABASE_URL = os.getenv(
    "DATABASE_URL",
    f"mysql+pymysql://{MYSQL_USER}:{MYSQL_PASSWORD}@{MYSQL_HOST}:{MYSQL_PORT}/{MYSQL_DB}",
)

engine = create_engine(DATABASE_URL, pool_pre_ping=True)
SessionLocal = sessionmaker(autocommit=False, autoflush=False, bind=engine)
Base = declarative_base()


def get_db():
    db = SessionLocal()
    try:
        yield db
    finally:
        db.close()
```

```python title="models.py"
from sqlalchemy import Column, Integer, String

from database import Base


class ItemModel(Base):
    __tablename__ = "crud_react_items"

    id = Column(Integer, primary_key=True, autoincrement=True)
    value = Column(String(255), nullable=False)
```

```python title="schemas.py"
from typing import Optional

from pydantic import BaseModel


class ItemRequest(BaseModel):
    value: str


class ItemPatch(BaseModel):
    value: Optional[str] = None
```

```python title="crud.py"
from sqlalchemy.orm import Session

from models import ItemModel
from schemas import ItemPatch, ItemRequest


def get_items(db: Session) -> list[ItemModel]:
    return db.query(ItemModel).order_by(ItemModel.id).all()


def get_item(db: Session, item_id: int) -> ItemModel | None:
    return db.get(ItemModel, item_id)


def create_item(db: Session, item: ItemRequest) -> ItemModel:
    row = ItemModel(value=item.value)
    db.add(row)
    db.commit()
    db.refresh(row)
    return row


def replace_item(db: Session, item_id: int, item: ItemRequest) -> ItemModel | None:
    row = get_item(db, item_id)
    if row is None:
        return None
    row.value = item.value
    db.commit()
    db.refresh(row)
    return row


def update_item(db: Session, item_id: int, patch: ItemPatch) -> ItemModel | None:
    row = get_item(db, item_id)
    if row is None:
        return None
    if patch.value is not None:
        row.value = patch.value
    db.commit()
    db.refresh(row)
    return row


def delete_item(db: Session, item_id: int) -> bool:
    row = get_item(db, item_id)
    if row is None:
        return False
    db.delete(row)
    db.commit()
    return True


def seed_items(db: Session) -> None:
    if db.query(ItemModel).count() == 0:
        db.add_all([ItemModel(value="first"), ItemModel(value="second")])
        db.commit()
```

```python title="main.py"
from fastapi import Depends, FastAPI, HTTPException, status
from fastapi.middleware.cors import CORSMiddleware
from sqlalchemy.orm import Session

import crud
from database import Base, SessionLocal, engine, get_db
from schemas import ItemPatch, ItemRequest

Base.metadata.create_all(bind=engine)

with SessionLocal() as db:
    crud.seed_items(db)

app = FastAPI()

# The React dev server (Vite) runs on a different origin/port than this
# API, so the browser needs explicit permission to read the response.
app.add_middleware(
    CORSMiddleware,
    allow_origins=["http://localhost:5173", "http://127.0.0.1:5173"],
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)


@app.get("/items")
def list_items(db: Session = Depends(get_db)):
    return [{"id": row.id, "value": row.value} for row in crud.get_items(db)]


@app.get("/items/{item_id}")
def get_item(item_id: int, db: Session = Depends(get_db)):
    row = crud.get_item(db, item_id)
    if row is None:
        raise HTTPException(status_code=404, detail="Item not found")
    return {"id": row.id, "value": row.value}


@app.post("/items", status_code=status.HTTP_201_CREATED)
def create_item(item: ItemRequest, db: Session = Depends(get_db)):
    row = crud.create_item(db, item)
    return {"id": row.id, "value": row.value}


@app.put("/items/{item_id}")
def replace_item(item_id: int, item: ItemRequest, db: Session = Depends(get_db)):
    row = crud.replace_item(db, item_id, item)
    if row is None:
        raise HTTPException(status_code=404, detail="Item not found")
    return {"id": row.id, "value": row.value}


@app.patch("/items/{item_id}")
def update_item(item_id: int, patch: ItemPatch, db: Session = Depends(get_db)):
    row = crud.update_item(db, item_id, patch)
    if row is None:
        raise HTTPException(status_code=404, detail="Item not found")
    return {"id": row.id, "value": row.value}


@app.delete("/items/{item_id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_item(item_id: int, db: Session = Depends(get_db)):
    deleted = crud.delete_item(db, item_id)
    if not deleted:
        raise HTTPException(status_code=404, detail="Item not found")
```

**Frontend** — `example/7/react/src/App.jsx`, full UI:

```jsx title="example/7/react/src/App.jsx"
import { useEffect, useState } from "react";
import axios from "axios";

// Where the FastAPI backend is running.
const API_URL = "http://127.0.0.1:8000";

// axios throws for any non-2xx response, so every call below lives in a
// try/catch. This turns the thrown error into a readable message.
function errorMessage(action, err) {
  const status = err.response?.status;
  return status ? `Failed to ${action} (status ${status})` : `Failed to ${action}: ${err.message}`;
}

export default function App() {
  const [items, setItems] = useState([]); // rows from the database, each { id, value }
  const [value, setValue] = useState(""); // what's typed in the "add" box
  const [reached, setReached] = useState(null); // result of "reach by id"
  const [error, setError] = useState(null);

  const [editingId, setEditingId] = useState(null); // which row's id is being edited
  const [editValue, setEditValue] = useState(""); // that row's in-progress text

  // Load every row once when the page opens.
  useEffect(() => {
    loadItems();
  }, []);

  // READ — GET /items, every row.
  async function loadItems() {
    try {
      const response = await axios.get(`${API_URL}/items`);
      setItems(response.data);
      setError(null);
    } catch (err) {
      setError(errorMessage("load items", err));
    }
  }

  // CREATE — POST /items, inserts a new row. The database assigns the id.
  async function addItem(event) {
    event.preventDefault();
    if (value.trim() === "") return;

    try {
      await axios.post(`${API_URL}/items`, { value: value });
    } catch (err) {
      setError(errorMessage("add item", err));
      return;
    }

    setValue("");
    await loadItems();
  }

  // REACH — GET /items/{id}, one specific row by its real database id
  // (not its position in the list — deleting a row never renumbers
  // anyone else's id, unlike the plain-array version of this topic).
  async function reachItem(id) {
    try {
      const response = await axios.get(`${API_URL}/items/${id}`);
      setReached(response.data);
      setError(null);
    } catch (err) {
      setError(errorMessage(`reach id ${id}`, err));
      setReached(null);
    }
  }

  function startEditing(id, currentValue) {
    setEditingId(id);
    setEditValue(currentValue);
  }

  // UPDATE — PUT or PATCH /items/{id}, depending which button was
  // clicked. Both send the same body here (this row only has one
  // field), but the HTTP method itself is what differs.
  async function saveEdit(id, method) {
    try {
      await axios({
        method: method, // "PUT" or "PATCH"
        url: `${API_URL}/items/${id}`,
        data: { value: editValue },
      });
    } catch (err) {
      setError(errorMessage("update item", err));
      return;
    }

    setEditingId(null);
    await loadItems();
  }

  // DELETE — DELETE /items/{id}, removes that row.
  async function deleteItem(id) {
    try {
      await axios.delete(`${API_URL}/items/${id}`);
    } catch (err) {
      setError(errorMessage("delete item", err));
      return;
    }

    await loadItems();
  }

  return (
    <main style={{ maxWidth: 560, margin: "3rem auto", fontFamily: "sans-serif" }}>
      <h1>MySQL Store</h1>

      {error && <p style={{ color: "crimson" }}>{error}</p>}

      {/* CREATE form */}
      <form onSubmit={addItem} style={{ display: "flex", gap: "0.5rem" }}>
        <input
          value={value}
          onChange={(event) => setValue(event.target.value)}
          placeholder="Add a value"
          style={{ flex: 1, padding: "0.5rem" }}
        />
        <button type="submit">Add</button>
      </form>

      {/* READ: every row, one <li> per row via .map() */}
      <ul style={{ listStyle: "none", padding: 0, marginTop: "1.5rem" }}>
        {items.map((item) =>
          editingId === item.id ? (
            <li
              key={item.id}
              style={{ display: "flex", alignItems: "center", gap: "0.5rem", padding: "0.25rem 0" }}
            >
              <span style={{ color: "#888", width: "2rem" }}>#{item.id}</span>
              <input
                value={editValue}
                onChange={(event) => setEditValue(event.target.value)}
                style={{ flex: 1, padding: "0.25rem" }}
              />
              <button onClick={() => saveEdit(item.id, "PUT")}>Save (PUT)</button>
              <button onClick={() => saveEdit(item.id, "PATCH")}>Save (PATCH)</button>
              <button onClick={() => setEditingId(null)}>Cancel</button>
            </li>
          ) : (
            <li
              key={item.id}
              style={{ display: "flex", alignItems: "center", gap: "0.5rem", padding: "0.25rem 0" }}
            >
              <span style={{ color: "#888", width: "2rem" }}>#{item.id}</span>
              <span style={{ flex: 1 }}>{item.value}</span>
              {/* REACH: fetches this one id directly from the backend,
                  instead of just reading it from what's already in state */}
              <button onClick={() => reachItem(item.id)}>Reach</button>
              <button onClick={() => startEditing(item.id, item.value)}>Edit</button>
              <button onClick={() => deleteItem(item.id)}>Delete</button>
            </li>
          ),
        )}
      </ul>

      {items.length === 0 && !error && <p>No rows yet — add something above.</p>}

      {reached && (
        <p style={{ marginTop: "1rem" }}>
          Reached id <strong>#{reached.id}</strong>: <strong>{reached.value}</strong>
        </p>
      )}
    </main>
  );
}
```
